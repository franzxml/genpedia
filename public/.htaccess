<IfModule mod_rewrite.c>
  Options -Multiviews
  RewriteEngine On

  # Ubah RewriteBase menjadi slash saja karena ini sekarang adalah root
  RewriteBase / 

  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteRule ^(.+)$ index.php?url=$1 [QSA,L]
</IfModule>